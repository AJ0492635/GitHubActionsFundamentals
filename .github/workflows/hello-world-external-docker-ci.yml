name: CI Build for EXTERNAL Docker Action
on: [workflow_dispatch]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: AJ0492635/hello-world-docker-action@v1
        with:
          who-to-greet: '@octocat'
